.jumbotron
  %h1 Browse listener playlists
  %ul.list-unstyled
    - @listener.each do |listener|
      %li.h3= link_to(listener.username, listener_path(listener.id))
      %li= link_to(image_tag(listener.image, class: "img-med"),listener_path(listener))
      %li
        - listener.podcasts.each do |podcast|
          %li.h4= link_to(image_tag(podcast.image, class: "img-med"),podcast_path(podcast))



